# Frontend

Interface React/Vite baseada no visual exportado pelo Google Stitch.

```powershell
npm install
npm run dev
npm run build
```

Por padrao, a aplicacao consome a API em `http://127.0.0.1:8080`.
